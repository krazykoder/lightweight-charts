# Legends Examples Walkthrough

Implemented a new examples page `legends.html` to demonstrate robust legend implementations for various chart types.

## Changes Made

### Examples Website

#### [NEW] [legends.html](file:///Users/towshif/code/js/lightweight-charts/examples/website/legends.html)
- Implemented an interactive playbook for exploring legend configurations.
- Added hardcoded examples for single-series line legends, OHLC candlestick legends, and multi-series stacked legends.
- Used `subscribeCrosshairMove` for real-time data updates.
- Applied modern CSS with Glassmorphism effects for the legends.

#### [MODIFY] [index.html](file:///Users/towshif/code/js/lightweight-charts/examples/website/index.html)
- Added link to the new `legends.html` page.

## Verification Results

### Automated Tests
- None.

### Manual Verification
> [!IMPORTANT]
> Browser verification skipped as per user request. 
> The implementation follows the established patterns in the codebase (e.g., using `app-data.js` and `subscribeCrosshairMove`).

## Proof of Work

### Legends Playbook
The playbook allows switching between different series types and legend styles.

```javascript
chart.subscribeCrosshairMove(param => {
    if (!param || !param.time || param.point.x < 0 || param.point.y < 0) {
        // Handle reset/default state
        return;
    }
    legendElement.innerHTML = getLegendContent(seriesInfos, param);
});
```

### Stacked Multi-Series Legend
Demonstrates handling multiple series types (Price, Volume, Indicator) simultaneously.

render_diffs(file:///Users/towshif/code/js/lightweight-charts/examples/website/legends.html)
render_diffs(file:///Users/towshif/code/js/lightweight-charts/examples/website/index.html)
